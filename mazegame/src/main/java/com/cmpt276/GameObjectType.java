package com.cmpt276;

/**
 * Enum to represent the different subtypes of GameObjects.
 */
public enum GameObjectType {
    Barrier, Player, MobileEnemy, StaticEnemy, Pickup, SpecialPickup, ExitTile
}
